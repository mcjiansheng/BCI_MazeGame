# PyTorch should match the local CUDA runtime. The current workstation uses
# torch 2.5.1+cu121; install it from pytorch.org before this file if needed.
mne==1.8.0
numpy>=1.26,<3
scipy>=1.11
scikit-learn>=1.3
pytest>=8

